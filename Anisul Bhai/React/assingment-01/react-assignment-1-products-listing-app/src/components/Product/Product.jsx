import React from 'react';

const Product = ({product}) => {
    console.log(product.product);
    return (
        <div>
            <h1>this is a validation mood today </h1>
        </div>
    );
};

export default Product;